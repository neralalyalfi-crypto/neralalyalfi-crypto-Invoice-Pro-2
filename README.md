# InvoicePro

Flutter invoice manager with invoice creation, automatic subtotal/GST/total calculation, SQLite storage, PDF printing/sharing, and invoice history.

## Run

flutter pub get
flutter run

Data is stored locally on the device using SQLite.
